<h1><%= @user.username %>'s created events</h1>

<% if @user.hosted_events.empty? %>
    <p><%= @user.username %> does not have any hosted events.</p>
<% else %>
    <%= render partial:'application/event', collection: @user.hosted_events %>
<% end %>

